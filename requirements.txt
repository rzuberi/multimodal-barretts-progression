# ANALYSIS dependencies for the Barrett's Chapter 1 pipeline.
# Frozen from the cluster `erin` conda environment (Python 3.10.19), which is
# the environment the README reproduce commands use. Regenerate with:
#   /home/zuberi01/miniforge3/envs/erin/bin/python -m pip freeze
#
# NOTE: the frozen model checkpoints were trained under the `virchow2` env
# (scikit-learn 1.7.2) — see environment-training.yml. Use that env to load or
# re-run the pickled models; use these pins for analysis/figures.
#
# GPU builds of torch/torchvision are installed via conda on the cluster
# (see environment.yml); the versions below match that environment.
h5py==3.8.0
joblib==1.5.3
matplotlib==3.10.8
numpy==1.26.4
pandas==2.2.3
pillow==12.1.0
PyYAML==6.0.3
scikit-learn==1.5.2
scipy==1.15.3
seaborn==0.13.2
timm==1.0.3
torch==2.0.1
torchvision==0.15.2
